import React from "react";
import styled from "styled-components";

const SideLeftComponent = () => {
  return <SLCWrapper></SLCWrapper>;
};

const SLCWrapper = styled.section``;

export default SideLeftComponent;
